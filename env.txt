# Delete existing process
pm2 stop letters-frontend

npm run build

pm2 restart letters-frontend

pm2 save

# Restart backend
sudo systemctl restart letters-backend

sudo systemctl status letters-backend

# Restart Nginx
sudo systemctl restart nginx

# Start on a different port (e.g., 3001)
PORT=3001 pm2 start npm --name "letters-frontend" -- start

# Save
pm2 save

curl http://localhost:10000

